{
  "getAll": ["select mopedid, name, itemsInStock, topSpeed, modelYear"],
  "get": ["select mopedid, name, itemsInStock, topSpeed, modelYear", "where id=?"],
  "insert": [
    "insert into moped (mopedid, name, itemsInStock, topSpeed, modelYear)",
    "values(?,?,?,?,?)"
  ],
  "update": [
    "update moped set name=?, itemsInStock=?, topSpeed=?, modelYear=?",
    "where mopedid=?"
  ],
  "remove": ["delete from moped where mopedid=?"],
  "primaryKey": "mopedid",
  "resource": "mopeddb"
}
